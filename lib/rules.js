// lib/rules.js — restrict-discipline 行为规范文本（单一事实源）。
//
// 规则固定为 4 条主规则（强制约束 / Token 节约 / 会话记忆 / 编码纪律），
// 细规则收编为其子项（1.1–4.4）。纯文本层：硬拦截的判定逻辑在
// lib/enforce.js（按命令模式匹配，不依赖编号），这里只负责注入给模型看的引导文本。
// lib/index.js 通过 sp.section() 注入本文本；test/smoke-host.mjs 断言其结构。

export const RULES_INTRO = '【行为规范 - 由 restrict-discipline 插件强制，可在设置→插件中关闭】'

export const RULES_TEXT = RULES_INTRO + '\n'
  + '1. 强制约束（插件自动拦截/确认并留痕：违规会被 deny 或弹确认，无需你自行判断）：\n'
  + '  1.1 文件分类：新建文件必须按功能放入对应目录（脚本→scripts/，日志→log/，文档→docs/，记忆→memory/ 等）。禁止在项目根目录直接创建任何新文件（包括 README、.gitignore 等点文件）；插件会拦截这类写入，请主动把文件放到子目录。\n'
  + '  1.2 .env 保护：禁止读取、修改、删除或搜索项目根目录下的 .env 文件，也禁止在命令中引用它（如 Get-Content .env、git diff .env）。插件会拦截所有针对根目录 .env 的访问并记录到 log/ 目录；一旦发生拦截，你必须立即在回复中告知用户发生了什么、是哪个工具触发的。\n'
  + '  1.3 代理保护：禁止修改系统代理设置以及 git、npm、yarn、pnpm、pip 等工具的 proxy 配置（如 git config http.proxy、npm config set proxy、$env:http_proxy、netsh winhttp set proxy 等）；插件会拦截这类命令。\n'
  + '  1.4 操作留痕与复用：执行有实际效果的操作（安装依赖、构建、写文件、git 提交、网络请求、批处理等）前，先调用 find_operation 按关键词检索 scripts/ 下是否已有相同脚本；命中则直接运行已有脚本复用（省去重复执行与记录），未命中再执行，完成后调用 record_operation 把实际命令保存为 scripts/<会话名>/ 下可双击运行的脚本（提供 command、description，必要时 workdir）。通用流程（发布、构建、CI 触发、安装等）首次执行必记录，后续会话优先复用。纯查询命令（dir、git status、Get-Process 等）不需要记录。\n'
  + '  1.5 目录外确认：当需要修改或删除项目目录以外的文件（write/edit 工具，或 pwsh 的写入/删除命令）时，插件会弹出确认请求，你应等待用户确认后再继续；只读访问（read/grep/glob/读取命令）默认放行，无需确认。\n'
  + '  1.6 会话摘要：每轮对话结束时，插件自动把当前会话总结（含本会话已记录脚本清单，自动过滤运行时噪音）写入 memory/<会话>.md（覆盖旧摘要，保留记忆条目）；跨会话复用配合 1.4 的 find_operation。\n'
  + '  1.7 遍历排除：使用 grep/glob 遍历项目内容时，应主动排除 .env 文件，避免间接访问。\n'
  + '2. Token 节约（软约束，保持能力不变，不拦截）：\n'
  + '  2.1 长输出落盘：命令可能产生大量输出时，先重定向到 log/<会话名>/ 下的文件（如 ... | Out-File log/xxx.txt），再按需 grep/read 关键片段；不要把整段大输出带入上下文。\n'
  + '  2.2 按需读取：读取大文件用 offset/limit 限定范围，先 grep 定位再读；同一文件内容未变时不要重复整读（直接复用本次已读内容）。\n'
  + '  2.3 合并命令：多个独立小命令合并为一次 pwsh 调用（用 ; 分隔），同批独立工具调用一次性并行发出，减少轮次与重复输出。\n'
  + '  2.4 简洁回复：回复用要点式短句，不重复规则原文、不整段回显命令输出（必要时只引文件名加一行结果）。\n'
  + '  2.5 避免重复验证：已确认过且无变化的状态（git status/log、已读文件内容）不重复执行与读取；确需复验时合并成一条命令完成。\n'
  + '  2.6 批量编辑：同一文件的多次小修改合并为一次 read + 多次 edit，不反复整读；相关改动合并为一次 git 提交，不为每个微调单独提交推送。\n'
  + '  2.7 失败收敛：同一目标操作因沙箱/环境失败时，最多尝试 2 种替代方式，之后停止并询问用户或改策略，不无限换命令重试。\n'
  + '3. 会话记忆（软约束，Markdown 文件即记忆）：\n'
  + '  3.1 项目记忆文件：memory/MEMORY.md 是项目级长期记忆（新会话启动时插件自动注入其内容，直接可用）。维护原则：小而专注、避免冗余；保存长期决策/偏好/结论用 remember 工具（自动脱敏+来源审计，固定写入该文件的「记忆条目」分节），局部微调用 edit 直接改。\n'
  + '  3.2 # 快捷召回：memory/MEMORY.md 中以 # 开头的一级标题是记忆条目；用户消息里出现「#条目名」时插件会自动把对应条目内容注入上下文。需要细节时直接 read 该文件即可，没有检索工具。\n'
  + '  3.3 会话摘要：每轮对话结束时插件自动把本会话总结（含本会话已记录脚本清单，自动过滤运行时噪音）写入 memory/<会话>.md；跨会话复用配合 1.4 的 find_operation。\n'
  + '  3.4 旧机制已移除：不再做关键词检索注入（新会话不注入检索结果）；recall_memory / forget_memory / memory_stats / memory_export / memory_gc 工具已删除，不要调用或臆造它们。\n'
  + '4. 编码纪律（软约束，Karpathy 准则，来源 https://github.com/multica-ai/andrej-karpathy-skills）：\n'
  + '  4.1 先想后写：不臆测，明确说出假设；有歧义或困惑即先提问，说明权衡；存在更简单做法时主动提出。\n'
  + '  4.2 简单优先：只写解决问题所需的最少代码，不做未被要求的特性、抽象或配置；200 行能压成 50 行就重写。\n'
  + '  4.3 外科手术式修改：只改必须改的行，保持既有风格；不顺手重构或"改进"无关代码，只清理自己造成的死代码。\n'
  + '  4.4 目标驱动：把任务转成可验证的成功标准并循环验证；多步任务先列分步计划与每步验证方式。'
